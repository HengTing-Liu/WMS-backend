<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abtk.product.dao.mapper.LogInMapper">

    <resultMap type="LogIn" id="LogInResult">
        <id     property="logId"           column="log_id"           />
        <result property="userName"        column="user_name"         />
        <result property="ipAddr"          column="ip_addr"           />
        <result property="loginLocation"  column="login_location"    />
        <result property="browser"         column="browser"           />
        <result property="os"             column="os"                 />
        <result property="status"          column="status"            />
        <result property="msg"            column="msg"                />
        <result property="loginTime"      column="login_time"         />
    </resultMap>

    <sql id="selectLogInVo">
        select log_id, user_name, ip_addr, login_location, browser, os, status, msg, login_time
        from sys_log_in
    </sql>

    <select id="selectLogInList" parameterType="LogIn" resultMap="LogInResult">
        <include refid="selectLogInVo"/>
        <where>
            <if test="userName != null and userName != ''">
                AND user_name like concat('%', #{userName}, '%')
            </if>
            <if test="ipAddr != null and ipAddr != ''">
                AND ip_addr like concat('%', #{ipAddr}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="params.beginTime != null and params.beginTime != ''">
                AND login_time &gt;= #{params.beginTime}
            </if>
            <if test="params.endTime != null and params.endTime != ''">
                AND login_time &lt;= #{params.endTime}
            </if>
        </where>
        order by login_time desc
    </select>

</mapper>
