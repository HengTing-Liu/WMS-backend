<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abtk.product.dao.mapper.InvQrcodedetailMapper">

    <resultMap type="com.abtk.product.dao.entity.InvQrcodedetail" id="InvQrcodedetailMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="createBy" column="create_by" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateBy" column="update_by" jdbcType="VARCHAR"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="isDeleted" column="is_deleted" jdbcType="INTEGER"/>
        <result property="remarks" column="remarks" jdbcType="VARCHAR"/>
        <result property="qrcode" column="qrcode" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="InvQrcodedetailMap">
        select
id, create_by, create_time, update_by, update_time, is_deleted, remarks, qrcode
        from inv_qrcodedetail
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAll" resultMap="InvQrcodedetailMap">
        select
id, create_by, create_time, update_by, update_time, is_deleted, remarks, qrcode
        from inv_qrcodedetail
        <where>
                <if test="id != null">
                    and id = #{id}
                </if>
                <if test="createBy != null and createBy != ''">
                    and create_by = #{createBy}
                </if>
                <if test="createTime != null">
                    and create_time = #{createTime}
                </if>
                <if test="updateBy != null and updateBy != ''">
                    and update_by = #{updateBy}
                </if>
                <if test="updateTime != null">
                    and update_time = #{updateTime}
                </if>
                    and is_deleted = '0'
                <if test="remarks != null and remarks != ''">
                    and remarks = #{remarks}
                </if>
                <if test="qrcode != null and qrcode != ''">
                    and qrcode = #{qrcode}
                </if>
        </where>
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from inv_qrcodedetail
        <where>
                <if test="id != null">
                    and id = #{id}
                </if>
                <if test="createBy != null and createBy != ''">
                    and create_by = #{createBy}
                </if>
                <if test="createTime != null">
                    and create_time = #{createTime}
                </if>
                <if test="updateBy != null and updateBy != ''">
                    and update_by = #{updateBy}
                </if>
                <if test="updateTime != null">
                    and update_time = #{updateTime}
                </if>
                    and is_deleted = '0'
                <if test="remarks != null and remarks != ''">
                    and remarks = #{remarks}
                </if>
                <if test="qrcode != null and qrcode != ''">
                    and qrcode = #{qrcode}
                </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into inv_qrcodedetail(create_by, create_time, update_by, update_time, is_deleted, remarks, qrcode)
        values (       
        #{createBy}
,        
        #{createTime}
,        
        #{updateBy}
,        
        #{updateTime}
,        
        <choose>
            <when test="isDeleted != null">
                #{isDeleted}
            </when>
            <otherwise>
                0
            </otherwise>
        </choose>
,        
        #{remarks}
,        
        #{qrcode}
)
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into inv_qrcodedetail(create_by, create_time, update_by, update_time, is_deleted, remarks, qrcode)
        values
        <foreach collection="entities" item="entity" separator=",">
        (            #{entity.createBy}
,
            #{entity.createTime}
,
            #{entity.updateBy}
,
            #{entity.updateTime}
,
            <choose>
                <when test="entity.isDeleted != null">
                    #{entity.isDeleted}
                </when>
                <otherwise>
                    0
                </otherwise>
            </choose>
,
            #{entity.remarks}
,
            #{entity.qrcode}
)
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into inv_qrcodedetail(create_by, create_time, update_by, update_time, is_deleted, remarks, qrcode)
        values
        <foreach collection="entities" item="entity" separator=",">
            (            #{entity.createBy}
,             #{entity.createTime}
,             #{entity.updateBy}
,             #{entity.updateTime}
,             <choose>
                <when test="entity.isDeleted != null">
                    #{entity.isDeleted}
                </when>
                <otherwise>
                    0
                </otherwise>
            </choose>
,             #{entity.remarks}
,             #{entity.qrcode}
)
        </foreach>
        on duplicate key update
create_by = values(create_by),
create_time = values(create_time),
update_by = values(update_by),
update_time = values(update_time),
is_deleted = values(is_deleted),
remarks = values(remarks),
qrcode = values(qrcode)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update inv_qrcodedetail
        <set>
            <if test="createBy != null and createBy != ''">
                create_by = #{createBy},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateBy != null and updateBy != ''">
                update_by = #{updateBy},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="isDeleted != null">
                is_deleted = #{isDeleted},
            </if>
            <if test="remarks != null and remarks != ''">
                remarks = #{remarks},
            </if>
            <if test="qrcode != null and qrcode != ''">
                qrcode = #{qrcode},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        update  inv_qrcodedetail  set is_deleted = '1',update_by=#{username}, update_time = now(),remarks = CONCAT(remarks, '|', #{username}, '删除', NOW()) where id = #{id}
    </delete>


    <delete id="deleteBatchByIds">
        update inv_qrcodedetail
        set is_deleted = '1',update_by=#{username}, update_time = now(),remarks = CONCAT(remarks, '|', #{username}, '删除', NOW())
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    
<!-- 批量修改数据（通过主键）-->
<update id="updateBatch" parameterType="java.util.List">
    <foreach collection="list" item="item" index="index" separator=";">
        update inv_qrcodedetail
        <set>
            <if test="item.createBy != null and item.createBy != ''">
                create_by = #{item.createBy},
            </if>
            <if test="item.createTime != null">
                create_time = #{item.createTime},
            </if>
            <if test="item.updateBy != null and item.updateBy != ''">
                update_by = #{item.updateBy},
            </if>
            <if test="item.updateTime != null">
                update_time = #{item.updateTime},
            </if>
            <if test="item.isDeleted != null">
                is_deleted = #{item.isDeleted},
            </if>
            <if test="item.remarks != null and item.remarks != ''">
                remarks = #{item.remarks},
            </if>
            <if test="item.qrcode != null and item.qrcode != ''">
                qrcode = #{item.qrcode},
            </if>
        </set>
        where id = #{item.id}
    </foreach>
</update>

</mapper>

