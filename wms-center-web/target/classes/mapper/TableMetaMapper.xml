<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abclonal.product.dao.mapper.TableMetaMapper">

    <resultMap id="TableMetaResult" type="com.abclonal.product.dao.entity.TableMeta">
        <id property="id" column="id"/>
        <result property="tableCode" column="table_code"/>
        <result property="tableName" column="table_name"/>
        <result property="module" column="module"/>
        <result property="entityClass" column="entity_class"/>
        <result property="serviceClass" column="service_class"/>
        <result property="permissionCode" column="permission_code"/>
        <result property="pageSize" column="page_size"/>
        <result property="isTree" column="is_tree"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="isDeletedColumn" column="is_deleted_column"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByTableCode" resultMap="TableMetaResult">
        SELECT * FROM sys_table_meta
        WHERE table_code = #{tableCode} AND status = 1
    </select>

    <select id="selectAll" resultMap="TableMetaResult">
        SELECT * FROM sys_table_meta
        WHERE status = 1
        ORDER BY create_time DESC
    </select>

    <select id="selectByModule" resultMap="TableMetaResult">
        SELECT * FROM sys_table_meta
        WHERE module = #{module} AND status = 1
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_table_meta (
            table_code, table_name, module, entity_class, service_class,
            permission_code, page_size, is_tree, status, remark,
            is_deleted_column, create_by, create_time
        ) VALUES (
            #{tableCode}, #{tableName}, #{module}, #{entityClass}, #{serviceClass},
            #{permissionCode}, #{pageSize}, #{isTree}, #{status}, #{remark},
            #{isDeletedColumn}, #{createBy}, NOW()
        )
    </insert>

    <update id="update">
        UPDATE sys_table_meta
        SET table_name = #{tableName},
            module = #{module},
            permission_code = #{permissionCode},
            page_size = #{pageSize},
            is_tree = #{isTree},
            status = #{status},
            remark = #{remark},
            is_deleted_column = #{isDeletedColumn},
            update_by = #{updateBy},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_table_meta WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="TableMetaResult">
        SELECT * FROM sys_table_meta WHERE id = #{id}
    </select>

</mapper>
